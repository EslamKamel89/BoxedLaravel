networks:
  laravel:
    name: laravel
services:
  nginx:
    image: "nginx:stable-alpine"
    container_name: nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - laravel
  # php:
  # mysql:
  # composer:
  # artisan:
  # npm:
